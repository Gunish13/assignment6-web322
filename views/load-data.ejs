<%- include('partials/header') %>

<div class="container mx-auto px-4 py-8">
    <div class="bg-white rounded-lg shadow-md p-6">
        <h1 class="text-2xl font-bold mb-4">Load Data</h1>
        <div class="alert <%= message.includes('added') ? 'alert-success' : message.includes('already') ? 'alert-info' : 'alert-error' %>">
            <p><%= message %></p>
        </div>
        <a href="/inventory/list" class="btn btn-primary mt-4">Back to Products List</a>
    </div>
</div>

<%- include('partials/footer') %>